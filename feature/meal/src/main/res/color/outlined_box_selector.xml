<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Цвет обводки в состоянии фокуса -->
    <item android:color="?attr/android:textColor" android:state_focused="true"/>
    <!-- Цвет обводки в неактивном состоянии -->
    <item android:color="?attr/android:textColor"/>
</selector>